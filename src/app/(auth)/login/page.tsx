"use client";
import React from "react";

const LoginPage = () => {
  const handleSignin = async () => {
    
  };
  return (
    <div>
      <button onClick={handleSignin}>Login page</button>
    </div>
  );
};

export default LoginPage;
